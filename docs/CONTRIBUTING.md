## Adding a new component

1. Add types to @velum/core if needed
2. Add logic to @velum/core
3. Add React wrapper in @velum/react
4. Add motion via @velum/adapters if needed
5. Add example in docs app